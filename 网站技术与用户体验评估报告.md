# 网站技术与用户体验评估报告

> 审计日期：2026-08-14  
> 审计方式：只读静态源码分析、本地非破坏性验证、与现有 `评估报告.md` 对照核验  
> 内容边界：严格跳过与站点主题相关的成人、色情或露骨正文及图片，仅评估与内容主题无关的技术质量和通用用户体验  
> 变更范围：本报告文件；未修改网站代码、配置、数据、SQL、图片或其他文件

## 一、审计范围与方法

### 1. 审计范围

本次安全评估以下技术资产：

- 公共页面的通用 HTML 外壳：首页、搜索页及六个内容页。
- 管理页面 `Tsinhoht.html`。
- 生成与运维脚本：`scripts/gen.js`、`scripts/parse.js`、`scripts/ops_backup.js`、`scripts/send_mail.js`、`scripts/send_mail_alert.js`。
- JSON 配置与索引：`titles.json`、`timestamps.json`、`site.json`、`search_index.json`、`counts.json`、`links.json`、`games.json`、`pins.json`。
- XML 文件：`sitemap.xml`、`rss.xml`。
- Supabase 通用升级 SQL：`supabase/upgrade_comment_guard.sql`、`supabase/upgrade_notify_comment.sql`、`supabase/upgrade_replied.sql`。
- 通用导航、表单、弹窗、焦点、ARIA、响应式规则、静态资源路径、站内页面引用、缓存与加载策略。
- 现有 `评估报告.md` 中不涉及被排除内容的技术结论。

### 2. 明确排除范围

- 未读取、描述、分析或评价任何被排除的正文内容。
- 未打开、识别、描述或评价相关图片内容。
- 未评价内容主题、文本质量、图片质量或内容适宜性。
- 未对可能指向被排除内容的外部目标执行访问验证。
- 未修改、创建测试副本或执行生成脚本，以避免改写现有页面和数据。

### 3. 审计方法

- 按文件类型清点工作区中的 HTML、JavaScript、JSON、XML 和 SQL 文件。
- 先读取现有 `评估报告.md`，仅提取技术结构和通用体验结论，再以当前源码独立核验。
- 使用源码检索检查 HTML 语义、ARIA、焦点、响应式断点、动态交互、资源路径和 Supabase 调用路径。
- 使用 Node.js 语法检查验证独立 JavaScript 文件。
- 使用 `JSON.parse` 验证 JSON 文件的可解析性。
- 使用 `html-validate` 检查首页、搜索页和管理页的 HTML 结构及规范问题。
- 人工审查 XML 根结构及 Supabase SQL 的权限、数据完整性、并发和错误处理逻辑。
- 所有结论均区分静态确认、工具验证和无法动态验证的事项，不将推测表述为已验证事实。

## 二、验证工具与结果

### 1. JavaScript 语法检查

执行了以下只读命令：

```text
node --check scripts\gen.js
node --check scripts\parse.js
node --check scripts\ops_backup.js
node --check scripts\send_mail.js
node --check scripts\send_mail_alert.js
```

结果：5 个文件均无语法错误输出，全部通过 Node.js 语法检查。该结果仅证明语法可解析，不证明浏览器运行路径、网络请求或业务逻辑全部正确。

### 2. JSON 解析检查

使用 Node.js `JSON.parse` 检查以下 8 个文件：

- `titles.json`
- `timestamps.json`
- `site.json`
- `search_index.json`
- `counts.json`
- `links.json`
- `games.json`
- `pins.json`

结果：8 个文件均成功解析。该结果不等同于完成 JSON Schema 字段约束验证。

### 3. HTML 静态验证

执行：

```text
npx --yes html-validate "index.html" "search.html" "Tsinhoht.html"
```

结果：共报告 66 个错误。错误中既包含内联样式、按钮缺少显式 `type` 等规范或维护性问题，也包含管理页标签隐式闭合、结束标签顺序错误等真实 DOM 结构风险。不能把 66 项全部解释为 66 个独立用户缺陷，本报告按根因合并归类。

### 4. XML 检查

- `sitemap.xml` 和 `rss.xml` 已进行人工结构核验。
- 曾尝试使用 Prettier 检查，但工具无法为 `.xml` 自动推断解析器，因此未完成专用 XML 格式或 schema 验证。
- 此工具失败不代表 XML 无效，也不能据此证明 XML 完全有效。

### 5. 静态资源与源码检索

- 文件扫描仅发现 `favicon.webp`，未发现页面引用的 `favicon.jpg` 或 `apple-touch-icon.jpg`。
- 精确检索确认六个内容页使用裸 `<html>`，并包含 `maximum-scale=1`。
- 检索确认多处使用 `outline:none`，弹窗结构中未发现完整 dialog 语义与焦点管理实现。
- 检索确认评论提交在 RPC 返回 404 时回退到直接写入 `comments` 表。

### 6. 未成功的辅助检查

- 一个用于批量统计重复 ID、缺失 `alt` 和 `_blank` 安全属性的临时 Node.js 命令因 PowerShell 引号解析失败，未产生有效结果。
- 一个 `rg` 重复 ID 检索命令因正则引号解析失败，未产生有效结果。
- 上述失败未被用作任何肯定性问题结论。

## 三、执行摘要

本次确认 18 个独立问题：Critical 0 个、High 4 个、Medium 9 个、Low 5 个。

| 严重程度 | 数量 | 核心主题 |
|---|---:|---|
| Critical | 0 | 静态证据未确认可直接定级为 Critical 的问题 |
| High | 4 | 评论安全边界、管理凭据持久化、管理页 DOM 结构、模态框可访问性 |
| Medium | 9 | SQL 数据完整性与并发、语言与缩放、焦点、动态状态、资源引用、性能 |
| Low | 5 | CSS 重复、按钮类型、样式位置、内联样式、工程化检查 |

最高优先级不是视觉微调或 Service Worker，而是收紧评论写入边界、降低 GitHub Token 暴露面、修复管理页结构，并补齐模态交互的键盘和辅助技术支持。

由于本次没有连接实际 Supabase 项目、没有执行真实浏览器测试，也没有读取线上响应头，因此数据库权限部署状态、线上缓存策略、真实交互行为和性能指标均不作已验证声明。

## 四、完整问题清单

## Critical

本次未确认 Critical 问题。

这不代表部署环境不存在严重风险。仓库未提供完整数据库 schema、所有 RLS 策略及当前授权状态，本次也未连接实际 Supabase，因此无法确认部署层不存在数据库级 Critical 风险。

## High

### H1. 评论防护 RPC 不存在时回退为直接写表

**类别**：Supabase 安全、反滥用、服务端边界

**具体表现**：前端先调用 `guard_comment` RPC；若响应状态为 404，则调用 `postDirect()`，直接向 `/rest/v1/comments` 插入数据。SQL 文件注释也将这一回退描述为兼容行为。

**影响**：

- RPC 未部署、被误删或路由异常时，频率限制、重复提交限制和统一服务端校验全部失效。
- 如果实际数据库仍允许 `anon` 直接 INSERT，调用者可绕过前端并直接请求 REST API。
- 安全能力依赖“RPC 当前存在”，而不是由数据库权限强制保证。

**准确文件与行号**：

- `scripts/gen.js:597-602`：定义直接写入 `comments` 的 `postDirect()`。
- `scripts/gen.js:604-610`：RPC 返回 404 时执行直接写入。
- `supabase/upgrade_comment_guard.sql:6`：注释明确说明函数不存在时回退直插。
- `8.10.html:1569`：已生成页面包含直接评论表写入路径。

**验证方式**：静态追踪评论表单的 Promise 链，并检索 `/rest/v1/comments` 的 POST 调用。

**改进建议**：

- 删除 RPC 404 时的直接写表回退，RPC 不存在时明确失败并显示可恢复错误。
- 撤销 `anon` 对 `comments` 表的直接 INSERT 权限。
- 只允许通过受控 RPC 创建评论。
- 将函数、RLS、GRANT 和撤权操作纳入同一幂等迁移，并在部署后自动核验。

### H2. GitHub Token 长期保存在 `localStorage`

**类别**：凭据安全、管理后台

**具体表现**：管理页从 `localStorage` 恢复 Token，并在输入事件中持续写入。`localStorage` 默认跨会话保留且可被同源 JavaScript 读取。

**影响**：

- 同源页面一旦出现 XSS，Token 可能被读取。
- 关闭浏览器后 Token 仍可能保留。
- 管理页同时提供 HTML 输入与预览功能，使同源不可信 HTML 风险值得重点控制。

**准确文件与行号**：

- `Tsinhoht.html:415`：定义 Token 存储键。
- `Tsinhoht.html:425`：从 `localStorage` 恢复 Token。
- `Tsinhoht.html:427`：输入后写入 `localStorage`。
- `Tsinhoht.html:381-399`：提供 HTML 输入、模板载入及发布操作。

**验证方式**：静态检查 Token 的读取、保存及相关管理功能数据流。

**改进建议**：

- 至少改为 `sessionStorage`，或默认不持久化 Token。
- 使用最小权限、短生命周期 Token。
- 更理想的方案是通过受控后端或 GitHub App 完成提交，使浏览器不持有长期仓库凭据。
- 为管理入口配置严格 CSP，避免将不可信 HTML 注入同源 DOM。

### H3. 管理页存在真实 HTML 闭合和 DOM 层级错误

**类别**：HTML 规范、DOM 可靠性、管理交互

**具体表现**：`html-validate` 报告 `<html>` 和 `<body>` 被文档结尾隐式闭合，且 `</main>`、`</body>`、`</html>` 出现时仍有未闭合元素。

**影响**：

- 浏览器会自动修复 DOM，不同解析器可能形成不同结构。
- 面板显示、CSS 选择器、事件绑定、焦点顺序和屏幕阅读器结构可能受到影响。
- 这属于实际结构风险，不只是格式偏好。

**准确文件与行号**：

- `Tsinhoht.html:163`：`<body>` 开始。
- `Tsinhoht.html:173`：`<main>` 开始。
- `Tsinhoht.html:404`：验证器判定 `</main>` 出现时仍有开放元素。
- `Tsinhoht.html:1623-1624`：`</body>` 和 `</html>` 同样存在闭合顺序问题。

**验证方式**：使用 `html-validate` 检查，命中 `no-implicit-close` 和 `close-order` 规则。

**改进建议**：

- 从第一个未配对容器开始修复，不应只在文档末尾补标签。
- 修复后重新验证完整管理页并在浏览器中检查实际 DOM 树。
- 在 CI 中启用 HTML 验证，将结构错误与纯风格规则分开配置。

### H4. 模态弹窗缺少完整对话框语义和焦点管理

**类别**：键盘导航、ARIA、屏幕阅读器、通用交互

**具体表现**：公告、邮箱提示和管理预览均使用普通容器模拟模态框。静态检索未发现完整的 `role="dialog"`、`aria-modal`、可访问名称、打开后聚焦、Tab 焦点约束、Escape 关闭和关闭后焦点恢复组合。

**影响**：

- 屏幕阅读器可能不知道对话框已打开。
- 键盘用户可能继续 Tab 到遮罩后的内容。
- 关闭弹窗后可能丢失操作上下文。

**准确文件与行号**：

- `scripts/gen.js:168-175`：公告弹窗使用普通 `div` 结构。
- `scripts/gen.js:494-499`：邮箱提示使用普通 `div` 结构。
- `8.1.1.html:1750`：已生成页面中的邮箱提示容器。
- `Tsinhoht.html:405-409`：管理预览模态框结构。

**验证方式**：检索弹窗容器、ARIA 属性、键盘事件、`Escape` 和 `.focus()` 调用。

**改进建议**：

- 优先使用原生 `<dialog>`。
- 否则补充 dialog 角色、`aria-modal`、标题关联和描述关联。
- 打开时将焦点移入弹窗，约束 Tab 循环，支持 Escape 关闭。
- 关闭时将焦点恢复至触发控件，并对背景使用 `inert`。

## Medium

### M1. `guard_comment` 未验证回复对象存在及属于当前页面

**类别**：Supabase、数据完整性

**具体表现**：函数接受 `p_pid` 并直接写入 `comments.pid`，未在函数内确认父评论存在，也未确认父评论的 `url` 与 `p_url` 一致。

**影响**：

- 底层没有外键时可能形成孤儿回复。
- 即使存在外键，也可能形成跨页面回复关系。

**准确文件与行号**：

- `supabase/upgrade_comment_guard.sql:18`：函数接受 `p_pid`。
- `supabase/upgrade_comment_guard.sql:30-40`：仅校验 URL 长度、昵称、邮箱和内容。
- `supabase/upgrade_comment_guard.sql:57-59`：未经父项归属校验直接插入。

**验证方式**：审查 PL/pgSQL 参数校验和插入控制流。

**改进建议**：

- 增加 `comments.pid -> comments.id` 外键。
- RPC 内确认父评论存在且 `parent.url = p_url`。
- 根据业务删除策略明确使用 `ON DELETE CASCADE` 或 `ON DELETE SET NULL`。

### M2. 评论防刷计数存在并发竞态

**类别**：Supabase、并发一致性、反滥用

**具体表现**：函数先查询限制窗口内的计数，再插入评论和防刷日志。多个并发事务可能在任一事务写入前同时通过查询。

**影响**：并发请求可能突破 30 秒或 5 分钟限制，现有限流不属于严格原子限流。

**准确文件与行号**：

- `supabase/upgrade_comment_guard.sql:43-51`：执行计数和限制判断。
- `supabase/upgrade_comment_guard.sql:57-61`：之后才插入评论和日志。

**验证方式**：事务执行顺序静态分析；未执行数据库并发压测。

**改进建议**：

- 使用 advisory lock、专门限流行锁或可原子冲突的约束设计。
- 为限流键明确规范化策略，避免同一主体使用大小写或空白差异绕过。

### M3. 通知触发器吞掉全部异常

**类别**：可观测性、运维可靠性

**具体表现**：通知 HTTP 请求发生任何异常时执行 `exception when others then null`，不记录失败原因。

**影响**：邮件通知可能长期失效而无人察觉，评论插入仍表现为成功，故障无法追踪或重试。

**准确文件与行号**：

- `supabase/upgrade_notify_comment.sql:50-64`：执行通知 HTTP 请求。
- `supabase/upgrade_notify_comment.sql:65-67`：吞掉所有异常。

**验证方式**：审查 PL/pgSQL 异常分支。

**改进建议**：

- 保持通知失败不阻断评论插入，但将失败写入独立日志表。
- 记录请求 ID、异常文本、重试状态和时间。
- 为持续失败设置告警和日志保留策略。

### M4. 六个内容页缺少文档语言声明

**类别**：HTML 语义、国际化、屏幕阅读器

**具体表现**：六个内容页使用裸 `<html>`，没有 `lang` 属性。

**影响**：屏幕阅读器和浏览器无法可靠选择中文发音、断句、拼写和输入辅助规则。

**准确文件与行号**：

- `8.10.html:2`
- `8.11.html:2`
- `8.1.1.html:2`
- `8.12PC.html:2`
- `8.12pcaz.html:2`
- `qzt.html:2`

**验证方式**：精确检索仅包含 `<html>` 的行。

**改进建议**：在生成阶段统一输出 `<html lang="zh-CN">`，避免逐个修改生成文件后再次被覆盖。

### M5. 内容页通过 `maximum-scale=1` 限制浏览器缩放

**类别**：移动端可访问性、低视力支持

**具体表现**：六个内容页的 viewport 均包含 `maximum-scale=1`。

**影响**：部分移动浏览器中的低视力用户无法正常放大页面。

**准确文件与行号**：

- `8.10.html:7`
- `8.11.html:7`
- `8.1.1.html:7`
- `8.12PC.html:7`
- `8.12pcaz.html:7`
- `qzt.html:7`

**验证方式**：源码检索 `maximum-scale=1`。

**改进建议**：移除 `maximum-scale=1`，保留 `width=device-width, initial-scale=1`。

### M6. 多处移除焦点轮廓，替代样式覆盖不完整

**类别**：键盘导航、视觉可访问性

**具体表现**：搜索输入框和导出页面组件多处使用 `outline:none`。部分表单提供边框或阴影替代，但没有证据表明所有可交互控件均具备清晰一致的 `:focus-visible` 状态。

**影响**：键盘用户可能无法识别当前焦点位置。

**准确文件与行号**：

- `index.html:20`
- `search.html:15-16`
- `8.1.1.html:551`
- `8.1.1.html:617`
- `8.1.1.html:650`
- `8.1.1.html:678`
- `8.1.1.html:732`
- `8.1.1.html:1032`
- `8.1.1.html:1103`
- `Tsinhoht.html:38`

同类导出样式还出现在其他内容页的对应位置。

**验证方式**：检索 `outline:none`、`:focus` 和 `:focus-visible`。

**改进建议**：

- 只有在提供等效且高对比的焦点指示时才移除默认轮廓。
- 统一使用 `:focus-visible`。
- 覆盖按钮、链接、输入框、横向导航和关闭按钮，并执行纯键盘遍历测试。

### M7. 动态搜索结果和状态缺少实时区域

**类别**：屏幕阅读器、异步交互

**具体表现**：搜索数量、空状态、错误状态和“加载更多”后的结果通过 JavaScript 更新，但相关容器没有静态证据显示配置了 `aria-live`、`role="status"` 或 `aria-busy`。

**影响**：屏幕阅读器用户可能不知道结果数量已变化、加载已完成或发生错误。

**准确文件与行号**：

- `search.html:76-88`：追加结果并动态创建“加载更多”按钮。
- `search.html:89-98`：动态更新计数、空状态和错误状态。
- `scripts/gen.js:868-869`：生成模板中的计数与结果容器。

**验证方式**：检查动态更新目标和 ARIA 属性检索结果。

**改进建议**：

- 计数和错误状态使用 `role="status"` 或 `aria-live="polite"`。
- 加载期间在结果容器设置 `aria-busy="true"`，结束后恢复。
- 加载更多后保持合理焦点和阅读顺序。

### M8. 页面引用不存在的图标资源

**类别**：静态资源引用、加载可靠性

**具体表现**：内容页引用 `favicon.jpg` 和 `apple-touch-icon.jpg`，工作区文件扫描只发现 `favicon.webp`。

**影响**：浏览器可能产生不必要的 404；Apple 设备无法取得声明的 JPG 触控图标。

**准确文件与行号**：

- `8.1.1.html:1018-1021`
- 其他五个内容页的对应引用位于相同的通用模板区域。
- 文件清单中仅发现根目录 `favicon.webp`。

**验证方式**：将 HTML 本地资源引用与工作区文件清单进行静态比对。

**改进建议**：

- 删除不存在的 JPG 引用，或提供真实且适合触控图标的文件。
- 在生成和 CI 阶段校验所有本地 `src` 与 `href`。

### M9. 页面重复内嵌资源，且动画未尊重减少动态效果偏好

**类别**：性能、缓存效率、动态效果可访问性

**具体表现**：内容页内嵌 base64 WOFF2 字体，并在多个页面重复注入 CSS 和 JavaScript；节点和弹窗使用动画，未发现 `prefers-reduced-motion` 适配。

**影响**：

- 页面间无法充分共享 CSS 和 JavaScript 缓存。
- HTML 传输和解析体积增加。
- 对动态效果敏感的用户无法通过系统偏好减少动画。

**准确文件与行号**：

- `8.12PC.html:21-23`：内嵌 WOFF2 字体示例。
- `scripts/gen.js:80`：节点动画。
- `scripts/gen.js:181-191`：公告弹窗动画。
- `scripts/gen.js:209`：逐项动画延迟生成。

**验证方式**：检查内嵌资源、动画声明和媒体查询；未执行网络瀑布或性能指标测试。

**改进建议**：

- 抽取共用 CSS 和 JavaScript 为带版本号的静态文件。
- 核实后移除没有实际用途的导出字体。
- 添加 `@media (prefers-reduced-motion: reduce)`。
- 先取得线上传输、缓存和渲染数据，再确定压缩和拆分优先级。

## Low

### L1. 首页存在重复 CSS 声明

**类别**：CSS 组织、生成质量

**具体表现**：`.site small` 连续声明两次，内容基本相同。

**影响**：当前视觉影响很小，但增加维护噪声，并反映模板可能存在重复来源。

**准确文件与行号**：`index.html:15-16`。

**验证方式**：比较相邻 CSS 规则。

**改进建议**：在生成模板中保留单一声明，并让生成结果检查捕获重复规则。

### L2. 多个按钮缺少显式 `type`

**类别**：HTML 规范、表单稳健性

**具体表现**：管理页标签、保存、关闭等按钮及首页部分按钮没有显式 `type="button"`。

**影响**：按钮未来被移动到表单中时，浏览器默认 `submit` 行为可能导致意外提交。

**准确文件与行号**：

- `Tsinhoht.html:178-183`
- `Tsinhoht.html:375`
- `Tsinhoht.html:397-398`
- `Tsinhoht.html:407`
- `Tsinhoht.html:412`
- `index.html:178`

**验证方式**：`html-validate` 的 `no-implicit-button-type` 规则。

**改进建议**：所有非提交按钮显式使用 `type="button"`，提交按钮显式使用 `type="submit"`。

### L3. `<style>` 被生成到 `<body>` 中

**类别**：HTML 规范、样式组织

**具体表现**：首页公告弹窗相关 `<style>` 位于 body 区域。

**影响**：浏览器通常能够容错，但不利于清晰文档结构和自动验证，也增加 CSP 管理难度。

**准确文件与行号**：

- `index.html:166-177`
- `scripts/gen.js:181-192`

**验证方式**：`html-validate` 的 `element-permitted-content` 规则及源码定位。

**改进建议**：将样式置于 `<head>`，或抽取到共享 CSS 文件。

### L4. 内联样式分散

**类别**：CSS 可维护性、CSP 兼容性

**具体表现**：首页、搜索页和管理页均存在 `style` 属性或通过 JavaScript直接设置样式。

**影响**：主题调整、响应式覆盖、暗色模式和严格 CSP 管理更困难。

**准确文件与行号**：

- `index.html:91`
- `search.html:53`
- `search.html:84-86`
- `Tsinhoht.html:394`

**验证方式**：`html-validate` 的 `no-inline-style` 结果及源码检查。

**改进建议**：逐步迁移到语义类名。该项不应早于安全边界和结构错误修复。

### L5. 缺少固定的前端质量检查配置

**类别**：工程质量、回归预防

**具体表现**：未找到 `package.json`、ESLint、Stylelint、HTML 校验或 Lighthouse 配置。

**影响**：生成页面中的结构、引用和可访问性回归主要依赖人工发现。

**准确文件与行号**：该问题属于文件级缺失，不适用单一行号。工作区模式扫描未发现相关配置文件。

**验证方式**：扫描 `package.json`、锁文件、ESLint、Stylelint、HTML 校验和 Lighthouse 常见配置文件名。

**改进建议**：

- CI 至少加入 `node --check`、JSON 解析、HTML 校验和本地资源引用检查。
- 将生成器输出纳入检查，而不只检查源模板。
- 固定工具版本，避免 `npx --yes` 使用浮动版本导致结果不稳定。

## 五、八个检查维度总结

### 1. HTML 语义与规范

- 首页和搜索页具有 `lang`、viewport 及基本语义容器。
- 六个内容页缺少 `lang`。
- 管理页存在真实闭合顺序错误。
- 首页存在 body 内 `<style>`。
- 多个按钮缺少显式类型，部分表单标签关系仍有改善空间。
- 内容页通用外壳仍大量依赖导出结构中的 `div` 和 `li`，语义层级有限。

**结论**：需要改进，管理页 DOM 结构应先于样式重构修复。

### 2. CSS 组织和视觉一致性

- 公共页面使用相近的颜色、圆角和排版体系，通用视觉语言基本一致。
- 大量样式按页面内嵌，内容页重复率高。
- 存在重复规则、内联样式和单一主要断点。
- 未发现减少动态效果偏好适配。

**结论**：视觉一致性尚可，组织、复用和可访问状态不足。

### 3. JavaScript 错误与效率

- 5 个独立 JavaScript 文件均通过 `node --check`。
- 此结果不覆盖内嵌脚本的全部运行路径。
- 部分异步请求使用空 `catch` 或吞错，故障可观测性不足。
- 评论提交按钮已有禁用锁，原报告关于缺少提交锁的结论已过时。
- 搜索会下载完整 JSON 并线性过滤，当前数据规模下不能静态证明存在性能故障，但数据增长后会退化。
- 页面脚本重复内嵌，不利于缓存复用。

**结论**：未发现独立脚本语法错误，安全边界、错误处理和资源复用更值得优先处理。

### 4. 导航和通用交互

- 首页、搜索页和内容页之间存在基础导航。
- 移动端导航支持横向滚动，但隐藏滚动条可能降低可发现性。
- 弹窗的指针交互具备基础实现，键盘和辅助技术支持不足。
- 搜索“加载更多”使用原生按钮，具备基础键盘激活能力。
- 至少存在一条 HTTP 导航目标，例如 `8.12pcaz.html:1123`。

**结论**：指针设备基础操作可用，键盘、状态反馈和安全链接治理仍需加强。

### 5. 响应式断点

- 主要布局断点集中在 `720px`。
- 移动端搜索、导航、评论表单和按钮会重排。
- 缺少明确的平板中间区间调优证据。
- 内容页限制缩放，对移动端低视力用户不利。

**结论**：具备基础移动适配，但尚不是经过多视口验证的完整响应式体系。

### 6. 键盘导航、ARIA 与屏幕阅读器

- 原生链接、按钮和表单提供一定基础能力。
- 六个内容页缺少文档语言声明。
- 模态框缺少完整 dialog 语义和焦点管理。
- 动态搜索状态缺少 live region。
- 多处焦点轮廓被隐藏。
- 未发现明确的跳过导航链接。

**结论**：这是当前最明显的通用用户体验短板之一。

### 7. 页面与资源加载性能、缓存策略

- 搜索结果图片使用 `loading="lazy"`。
- 内容页存在内嵌字体和重复 CSS、JavaScript。
- 未发现预加载、预取或 Service Worker。
- 缺少这些机制本身不自动构成缺陷，是否引入应以线上响应头和性能测量为依据。
- 图片未见自适应尺寸方案，且缺少统一的显式尺寸和解码策略。

**结论**：存在明确优化空间，但应先测量线上传输和渲染，再决定缓存及预加载方案。

### 8. 静态资源、链接、JSON/XML 与 Supabase

- 8 个 JSON 文件均可成功解析。
- `sitemap.xml` 具有 sitemap 根结构并列出公共页面。
- `rss.xml` 具有 RSS 2.0 根节点、channel、self link、item、guid 和 pubDate。
- XML 未通过专用 schema 验证器正式验证。
- 页面引用两个工作区不存在的 JPG 图标。
- Supabase SQL 使用 RLS 和 `security definer`，但评论回退路径、父项完整性、并发限流和通知可观测性需要加强。

**结论**：格式文件总体可解析，Supabase 权限边界和数据完整性应优先核验。

## 六、桌面、平板与移动端结论

### 1. 桌面端

- 固定宽度容器、顶部导航和基础页面布局从静态样式看具备桌面适配。
- 管理页 DOM 闭合错误可能造成浏览器解析差异，应优先修复。
- 模态框缺少焦点约束，纯键盘使用体验不完整。
- 重复内嵌资源会降低跨页面缓存效率，但本次未测量实际加载时间。

**结论**：基础布局可预期可用，但管理结构和键盘交互存在高优先级风险。未执行真实桌面浏览器动态验证。

### 2. 平板端

- 多数布局在宽度高于 720px 时继续使用桌面规则。
- 721px 附近存在明显模式切换，缺少中间宽度专门规则。
- 较窄平板上的导航和操作控件可能拥挤，但未执行截图测试，不能将其表述为已复现缺陷。
- 是否需要新增约 900px 断点应根据实际视口测试决定。

**结论**：平板适配证据不足，应补充真实视口回归，不应仅凭断点数量直接增加规则。

### 3. 移动端

- 720px 以下已有搜索换行、导航横向滚动、评论表单纵向排列和按钮重排。
- `maximum-scale=1` 是明确的移动无障碍问题。
- 焦点轮廓、模态焦点和动态状态播报不足。
- 图片未见 `srcset`，可能增加移动流量，但本次未测量图片传输体积。
- 横向导航隐藏滚动条，可能降低更多条目可发现性。

**结论**：基础移动布局存在，但低视力、键盘和屏幕阅读器体验仍需系统修复。未执行真实移动设备测试。

## 七、未验证项目及原因

- **相关正文与图片**：按任务边界主动跳过，未读取、描述或评价。
- **图片替代文字的内容准确性**：验证会涉及被排除内容，因此未检查。
- **Lighthouse、Core Web Vitals 和网络瀑布**：未启动真实浏览器或访问线上资源，不能提供动态性能分数。
- **真实键盘和屏幕阅读器行为**：仅完成静态源码检查，未使用 NVDA、JAWS、VoiceOver 或真实浏览器执行操作测试。
- **线上缓存头、压缩、CDN、HTTP/2 或 HTTP/3**：仓库源码不能证明部署响应头，未访问线上站点。
- **全部外部链接有效性**：部分目标可能涉及被排除内容，也可能受登录、地区和反自动化限制，因此未访问。
- **实际 Supabase RLS、GRANT 和表约束**：仓库仅包含增量 SQL，未连接部署数据库，也没有完整 schema。
- **旧评论数据的 `pid` 状态**：没有访问数据库，无法独立确认原报告的全库数据结论。
- **Supabase 并发限流实际绕过次数**：仅确认静态竞态结构，未进行并发压测。
- **XML schema 有效性**：Prettier 无法推断 XML 解析器，未使用 XSD 或专用验证器。
- **颜色对比度具体数值**：未对所有前景色、背景色、透明度和交互状态进行正式计算。
- **完整重复 ID、缺失 `alt` 和全部 `_blank` 属性统计**：辅助命令因 PowerShell 引号解析失败，没有有效结果，未据此下结论。
- **站内所有相对链接的浏览器级访问结果**：未启动本地服务器，也未执行页面抓取。
- **管理页 GitHub API 权限和发布流程**：未使用用户 Token，也未执行任何写操作。

## 八、修复路线图

### P0：立即处理

1. 删除评论 RPC 404 时的直接写表回退。
2. 核验并收紧 Supabase 权限，确保 `anon` 不能直接插入、更新或删除评论表。
3. 停止将 GitHub Token 长期保存在 `localStorage`。
4. 修复 `Tsinhoht.html` 的标签闭合和 DOM 层级错误。
5. 为评论 `pid` 增加外键及同页面父项校验。

### P1：短期处理

1. 为公告、邮箱提示和管理预览补齐模态语义与焦点管理。
2. 为六个内容页添加 `lang="zh-CN"`。
3. 移除 `maximum-scale=1`。
4. 统一可见的 `:focus-visible` 样式。
5. 修复不存在的 JPG 图标引用。
6. 为通知失败增加持久化日志和监控。
7. 使评论限流具备并发原子性。

### P2：中期处理

1. 抽取公共 CSS 和 JavaScript，减少页面重复内嵌。
2. 核实并移除无必要的内嵌字体。
3. 为搜索结果、加载和错误状态增加 `aria-live` 与 `aria-busy`。
4. 增加多尺寸缩略图、`srcset`、尺寸属性和一致的解码策略。
5. 增加本地静态资源和站内链接构建检查。
6. 对平板区间执行真实视口测试后再决定断点。

### P3：持续改进

1. 建立固定版本的 HTML、JSON、JavaScript、链接及生成结果 CI 检查。
2. 在取得线上缓存和性能数据后，再决定是否增加 Service Worker、preload 或 prefetch。
3. 增加真实浏览器键盘回归和辅助技术测试。
4. 建立可访问性与性能预算。
5. 为 Supabase 建立完整 schema 基线、迁移记录和部署后权限核验。
6. 逐步清理重复 CSS、内联样式和按钮隐式类型。

## 九、与原 `评估报告.md` 的结论对照

### 1. 已确认

- **CSS 重复声明**：确认，证据为 `index.html:15-16`。
- **内联样式较多**：确认，`html-validate` 和源码均有证据。
- **内容页存在内嵌字体**：确认，例如 `8.12PC.html:21-23`。
- **ARIA 和焦点管理不足**：确认，且模态框问题应高于原报告中的低优先级定位。
- **主要响应式断点集中于 720px**：确认，`scripts/gen.js:81-92` 等处可见。
- **未见自适应图片方案**：确认模板没有输出 `srcset` 或 `<picture>` 的静态证据。
- **存在 HTTP 导航目标**：确认，例如 `8.12pcaz.html:1123`。
- **未发现 Service Worker、preload 或 prefetch**：确认，但其缺失不应在无测量时自动视为高优先级缺陷。
- **模板仍硬编码载入特定页面**：确认，`Tsinhoht.html:398` 显示“载入 8.10 模板”。
- **旧评论数据不能仅靠当前前端修复**：判断合理，但实际数据状态本次未验证。

### 2. 已过时

- **“公告弹窗每次刷新都会出现”**：已过时。当前生成器已使用 `localStorage` 记录公告关闭状态，证据位于 `scripts/gen.js:179` 附近。
- **“表单提交缺少防抖或提交锁”**：已过时。`scripts/gen.js:596` 在提交时禁用按钮，`scripts/gen.js:617` 在 `finally` 中恢复。
- **“8.1.1 首页日期仍显示错误”**：当前静态 XML 输出不再支持这一结论。`sitemap.xml:7` 使用 `2026-08-11`，`rss.xml:30-34` 也使用对应日期。未执行完整生成回归，因此仅能确认当前输出，不对所有未来文件名格式作保证。

### 3. 原报告遗漏

- 评论防护 RPC 返回 404 时回退直接写表。
- GitHub Token 长期保存在 `localStorage`。
- 管理页存在真实 HTML 标签闭合和 DOM 层级错误。
- 六个内容页缺少 `lang`。
- 六个内容页通过 `maximum-scale=1` 限制缩放。
- `favicon.jpg` 和 `apple-touch-icon.jpg` 引用不存在。
- `guard_comment` 未验证父评论存在和页面归属。
- 评论限流存在并发竞态。
- 通知触发器吞掉全部异常。
- 动态搜索结果没有实时状态区域。
- 动画未适配 `prefers-reduced-motion`。

### 4. 存疑或需要重新定级

- **“Service Worker 应作为性能改进”**：可以研究，但不应在没有线上缓存和性能测量前列为优先事项。错误缓存策略可能导致长期陈旧内容。
- **“缺少预加载即为问题”**：不能仅根据 absence 判定。预加载错误资源可能争抢关键带宽。
- **“CSS/JavaScript 未压缩”**：可能影响传输，但必须结合部署压缩、静态托管行为和实际文件体积判断。
- **“颜色对比度不足”**：存在值得检查的颜色组合，但本次未完成正式对比度计算，暂列待验证。
- **“统计页面 HTML 重复元素”**：当前公开文件清单中未发现独立统计页，也没有取得足够行级证据，无法确认仍然成立。
- **“数据库旧 `pid` 全库污染”**：依赖数据库查询，本次无法独立确认。
- **“XML 完全有效”**：人工观察结构合理，但未通过专用 XML schema 验证器，不作完全有效的强结论。

## 十、问题数量统计

| 严重程度 | 数量 |
|---|---:|
| Critical | 0 |
| High | 4 |
| Medium | 9 |
| Low | 5 |
| **总计** | **18** |

本统计按独立根因合并计算，不将同一问题在多个生成页面中的重复实例分别计数，也不把 HTML 验证器的 66 条规则输出直接等同为 66 个独立问题。
