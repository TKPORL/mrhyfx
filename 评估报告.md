# MRHYFX 网站综合评估报告

> 评估时间：2026-08-14  
> 评估方式：静态源码分析 + 线上站点比对  
> 代码库版本：`1d5ab69`（已推送）  
> 线上地址：https://tkporl.github.io/mrhyfx/

---

## 目录

- [严重程度分级](#严重程度分级)
- [1. 功能完整性](#1-功能完整性)
- [2. 用户界面与视觉设计](#2-用户界面与视觉设计)
- [3. 交互体验](#3-交互体验)
- [4. 技术标准合规性](#4-技术标准合规性)
- [5. 响应式设计](#5-响应式设计)
- [6. 可访问性](#6-可访问性)
- [7. 性能优化](#7-性能优化)
- [8. 内容质量](#8-内容质量)
- [改进优先级总表](#改进优先级总表)

---

## 严重程度分级

| 级别 | 说明 |
|------|------|
| 🔴 **严重** | 影响核心功能、数据安全或用户无法完成主要操作 |
| 🟠 **较高** | 明显影响用户体验、存在潜在错误风险 |
| 🟡 **中等** | 体验不佳、代码可维护性差、但功能正常 |
| 🟢 **轻微** | 视觉/风格问题，建议优化 |

---

## 1. 功能完整性

### 1.1 🔴 评论区回复 pid 数据污染（已修复，但旧数据未清理）

**问题**：旧代码 `topId = c.pid || c.id` 导致所有"回复的回复"的 pid 被存为根评论 ID（而非实际回复对象）。数据库查询确认全库没有一条回复指向另一条回复。

**位置**：`scripts/gen.js:528-531`（显示逻辑）、`Tsinhoht.html:1340-1342`（显示逻辑）

**影响**：所有深层的"回复@某某"显示的是根评论作者，而非被回复者。前台后台均受影响。

**建议**：
- 新代码已修复（`replyPid = c.id`），但数据库旧数据需逐个手动修正
- 或提供 SQL 修复脚本，按回复内容人工指定目标 ID

### 1.2 🟠 公告弹窗：每次页面刷新都弹出

**位置**：`scripts/gen.js:162-189`、`index.html:147-174`

**问题**：公告弹窗没有设置 `localStorage` 缓存或 `sessionStorage` 控制，用户每次刷新页面都会看到弹窗。用户关闭后刷新又出现，非常烦人。

**影响**：重度用户每次访问都要关一次公告，体验较差。

**建议**：增加 `localStorage` 记录关闭状态，设置合理有效期（如 24h 或 7 天）。

### 1.3 🟠 搜索功能：仅支持中文精确匹配，不支持模糊/拼音/英文

**位置**：`search.html:90-98`、`scripts/gen.js:904-907`

**问题**：搜索使用 `indexOf` 精确匹配，不区分大小写但要求完整子串匹配。不支持拼音、同音字、英文关键词关联。

**影响**：用户输入"露露"搜不到"这就是露露的城市建设"之类的游戏名。

**建议**：引入简单的模糊搜索库（如 `fuse.js`）或实现拼音映射字典。

### 1.4 🟡 发布表单：模板载入强依赖 `8.10.html` 存在

**位置**：`Tsinhoht.html:847-848`、`Tsinhoht.html:1593`

**问题**：`buildFormHtml` 和"载入模板"按钮都硬编码 `fetch('8.10.html')`。如果 `8.10.html` 被删除，发布功能直接崩溃。

**影响**：一旦清理旧帖子，表单面板无法正常使用。

**建议**：增加 fallback 模板，或保存一份模板副本在 `assets/template.html`。

### 1.5 🟡 统计页面：HTML 结构有重复元素

**位置**：`Tsinhoht.html:328-338`

**问题**：`statTotalPosts` 和 `statAvgViews` 的 `.stat-box` 出现了两次——一次在 `stat-overview` 内部，一次在其外部，导致界面重复显示。

**影响**：帖子总数和平均访问会显示两次，页面混乱。

**建议**：删除 `</div>` 之后重复的 2 个 `.stat-box`。

### 1.6 🟢 访问统计：今日访问数据可能不准确

**位置**：`Tsinhoht.html:1466-1475`

**问题**：`inc_daily_view` 使用了 `localStorage` 去重（`gen.js:197-198`），但用户清除浏览器缓存或使用隐私模式会重新计数，导致今日访问数据偏高。

**影响**：访问统计仅供参考，不能作为精确的运营指标。

**建议**：在 `daily_page_views` 表增加 IP 去重逻辑（可选），或标注"仅供参考"。

---

## 2. 用户界面与视觉设计

### 2.1 🟠 颜色对比度不足

**问题**：
- `.pmeta` 和 `.pf` 使用 `#999` 颜色，在浅色背景上对比度仅 2.8:1，不满足 WCAG AA 标准（4.5:1）
- 统计卡片中的 `.stat-label` 使用 `#999` 同理
- 搜索页的 `placeholder` 文字使用默认灰色，对比度不足

**位置**：`index.html:43`、`Tsinhoht.html:107`、`Tsinhoht.html:141`

**建议**：将浅色文字改为 `#777` 或 `#888`，达到 4.5:1 对比度。

### 2.2 🟡 内联样式散落各处

**问题**：整个网站没有独立的 CSS 文件，CSS 分散在多个 `<style>` 标签中：
- 首页 `index.html:7-73` + `index.html:163-174`
- 帖子页通过 `gen.js:52-140` 注入 `sharedCss`
- 后台 `Tsinhoht.html:7-161`
- 搜索页 `search.html:7-37`
- 公告弹窗 CSS 单独一份 `index.html:163-174`

**影响**：修改一个样式需要改动 4-5 个位置，容易遗漏，维护成本高。

**建议**：将公共样式抽取为 `style.css`，各页面引用。gen.js 中的 `sharedCss` 可改为引用线上 CSS 文件。

### 2.3 🟡 帖子页布局：body 宽度限制不一致

**问题**：
- 原始帖子内容（`body.narrow` 最大宽度 790px）由 `gen.js:446` 注入
- 顶部导航栏 `.mrhx-bar` 最大宽度 `min(1000px, 100%)` 
- 首页内容最大宽度 900px
- 三个宽度不一致导致视觉上"跳变"

**位置**：`scripts/gen.js:53`（bar 1000px）、`scripts/gen.js:446`（body 790px）、`index.html:27`（main 900px）

**建议**：统一使用 900px 或 960px 作为内容宽度标准。

### 2.4 🟢 封面图片：hover 效果缺少过渡流畅性

**问题**：首页帖子卡片 hover 时 `.covers img` 使用 `transform: translateY(-2px)`，但 `.post` 同时有 `translateY(-3px)`，两层位移叠加导致视觉抖动。

**位置**：`index.html:36`（post hover）、`index.html:47`（covers img hover）

**建议**：去掉封面图片的独立 hover 位移，或统一为父容器控制。

### 2.5 🟢 后台管理：统计卡片布局溢出

**位置**：`Tsinhoht.html:138-139`、`Tsinhoht.html:308-338`

**问题**：`.stat-overview` 使用 `flex-wrap: wrap`，但 5 张卡片在窄屏（<480px）时每行显示 2 张，第 5 张单独一行，视觉效果不对称。

**建议**：在小屏下改为 3+2 或每行 1 张的布局。

---

## 3. 交互体验

### 3.1 🟠 评论加载无骨架屏或加载指示器

**位置**：`scripts/gen.js:578-582`

**问题**：评论区加载时只有 `list.textContent = ''` 清空区域，没有骨架屏或 loading 动画。用户看到的是空白区域闪烁。

**建议**：加载中显示"正在加载评论…"骨架屏。

### 3.2 🟠 表单提交：无防抖处理

**位置**：`Tsinhoht.html:1553-1574`（pubForm）、`Tsinhoht.html:1576-1587`（pubHtml）

**问题**：发布按钮虽然设置了 `disabled`，但如果用户快速双击、或网络延迟导致按钮状态恢复后再次点击，可能触发重复提交。

**影响**：可能导致重复发布同一帖子。

**建议**：增加防抖锁（flag），或在 finally 块中延时恢复按钮。

### 3.3 🟡 评论回复：无输入校验和字数提示

**位置**：`scripts/gen.js:584-611`

**问题**：评论表单没有字数统计和实时校验，用户可能输入超长内容（后端限制 2000 字但无前端提示）。

**影响**：用户写好长文后提交失败，体验差。

**建议**：增加 `maxlength` 属性和字数实时计数。

### 3.4 🟡 搜索："加载更多"按钮位置奇怪

**位置**：`search.html:83-88`、`scripts/gen.js:896-900`

**问题**：`moreBtn` 通过 `resBox.parentNode.insertBefore(moreBtn, resBox.nextSibling)` 插入到 `resBox` 之后，但 `resBox` 的父节点是 `main`，`nextSibling` 是 `footer`。按钮出现在搜索结果和页脚之间，位置不符合预期。

**建议**：将按钮插入到 `resBox` 内部作为最后一个子元素。

### 3.5 🟡 后台评论管理：加载大量评论时无分页

**位置**：`Tsinhoht.html:1331`

**问题**：`limit=2000` 一次性加载全部评论，随着评论量增长，加载时间会越来越长，且浏览器渲染 2000 条评论卡顿。

**影响**：当评论数达到数百条时，后台评论管理页签可能卡死。

**建议**：实现虚拟滚动或分页，每页加载 50 条。

---

## 4. 技术标准合规性

### 4.1 🔴 重复的 CSS 声明

**位置**：`index.html:15-16`

```css
.site small{font-size:11px;font-weight:400;color:#999;display:block;letter-spacing:0}
.site small{display:block;font-size:11px;font-weight:400;color:#999;letter-spacing:0}
```

**问题**：`display:block` 被声明了两次，完全相同的规则写了两遍。这是 gen.js 生成 index.html 时的模板拼接 bug（`gen.js:721-722`）。

**影响**：代码冗余，增加文件体积。

**建议**：修复 `gen.js` 中模板拼接逻辑，避免重复定义。

### 4.2 🟠 内联 onclick 事件处理

**问题**：多处使用 `onclick="..."` 属性内联事件处理：
- `index.html:149`：`onclick="document.getElementById('mrhxPopup').style.display='none'"`
- `index.html:160`：同上
- `scripts/gen.js:170`、`gen.js:175`：公告弹窗关闭按钮

**影响**：混合了表现和行为，违反了关注点分离原则，不利于维护。

**建议**：统一使用 `addEventListener` 绑定事件。

### 4.3 🟠 全局变量暴露

**问题**：`Tsinhoht.html` 中大量使用全局变量和函数（`$`, `TOKEN_KEY`, `REPO_KEY`, `show`, `esc`, 等），没有使用 IIFE 或模块封装。

**位置**：`Tsinhoht.html:423-1632`

**影响**：变量污染全局命名空间，可能导致命名冲突。

**建议**：将后台脚本用 IIFE 包裹，或使用 ES Module 方式组织。

### 4.4 🟡 缺少 `alt` 属性描述

**问题**：首页帖子卡片封面图片 `<img src="..." alt="" loading="lazy">` 的 `alt` 属性为空，不符合无障碍要求。

**位置**：`index.html:97`、`scripts/gen.js:693`

**建议**：为封面图片设置 `alt` 为游戏名称或"帖子封面"。

### 4.5 🟡 重复的 viewport 声明

**位置**：`qzt.html:4-7`（原始幕布导出）

**问题**：`<meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">` 和 `gen.js` 注入的 viewport 重复，且 `maximum-scale=1` 阻止用户缩放。

**影响**：可访问性差，视障用户无法放大页面。

**建议**：gen.js 的注入逻辑应覆盖或移除重复的 viewport 声明，并去掉 `maximum-scale=1`。

### 4.6 🟡 幕布导出样式污染

**位置**：`qzt.html:10-28`（原始幕布导出）

**问题**：幕布导出的 `@font-face` 使用 base64 内嵌字体（约 10KB），每条帖子页面都包含一份，总共 6 个页面 → 60KB 冗余。

**建议**：gen.js 在注入时移除幕布的自定义字体（`@font-face` 块），因为页面不使用幕布图标字体。

---

## 5. 响应式设计

### 5.1 🟡 断点单一（仅 720px）

**问题**：全站 CSS 仅有一个断点 `@media (max-width:720px)`，没有针对平板（768-1024px）和大屏（>1440px）的优化。

**影响**：在 iPad 等设备上，内容宽度可能过大或过小。

**建议**：增加 1024px 和 480px 两个断点，优化中等屏幕的阅读体验。

### 5.2 🟢 移动端：评论区缩进在窄屏上偏少

**位置**：`scripts/gen.js:136`

```css
@media (max-width:720px){.mrhx-creply-item{margin-left:10px}}
```

**问题**：10px 的缩进在移动端几乎无法区分层级，嵌套回复看起来像平铺。

**建议**：改为 `margin-left:16px` 或 `padding-left:10px`，保持视觉层次。

### 5.3 🟢 后台管理：标签页在窄屏上溢出

**位置**：`Tsinhoht.html:21-24`、`Tsinhoht.html:151`

**问题**：6 个标签页（发布/管理/评论/统计/链接/HTML）在 <480px 宽度下可能换行或溢出。

**建议**：在窄屏下使用左右滑动 tabs，或合并为下拉菜单。

---

## 6. 可访问性

### 6.1 🔴 缺少 ARIA 属性和语义化标签

**问题**：
- 导航栏使用 `<nav>` 但缺少 `aria-label`
- 公告弹窗缺少 `role="dialog"` 和 `aria-modal="true"`
- 评论区按钮缺少 `aria-label`
- 后台管理 tabs 使用 `<button>` 但缺少 `role="tab"` 和 `aria-selected`
- 搜索表单缺少 `role="search"`

**影响**：屏幕阅读器用户无法有效使用网站。

**建议**：为所有交互元素补充 ARIA 属性。

### 6.2 🟠 键盘焦点管理不完整

**问题**：
- 公告弹窗弹出后，焦点没有自动移到弹窗内
- 关闭弹窗后焦点没有回到触发按钮
- 评论回复按钮支持点击但缺少键盘操作提示
- 后台管理 tabs 不能通过左右箭头键切换

**建议**：实现焦点管理循环（focus trap），Tabs 增加键盘导航。

### 6.3 🟡 颜色作为唯一信息传达手段

**问题**：统计卡片仅通过背景色区分类型（`#fff7e6`=评论、`#fdf3f3`=访问等），色盲用户无法区分。同样，下载按钮仅通过颜色区分百度网盘（绿色）和移动云盘（红色）。

**建议**：为统计卡片增加图标前缀，为下载按钮增加文字标签区分。

---

## 7. 性能优化

### 7.1 🟠 页面体积过大（幕布导出内容）

**问题**：幕布导出的 `qzt.html` 页面包含巨大的 base64 字体文件（约 10KB 内嵌字体）和大量冗余的 CSS/HTML 结构。

**建议**：gen.js 在注入时执行以下优化：
- 移除 `@font-face` 内嵌字体块
- 移除重复的 `meta` 标签
- 精简冗余的 CSS 选择器

### 7.2 🟡 无图片懒加载外的性能优化

**问题**：除了 `loading="lazy"` 外，没有其他性能优化手段：
- 无资源预加载（`preload`/`prefetch`）
- 无 CSS 压缩（全部未压缩，含大量空格）
- 无 JS 压缩
- 无 Service Worker 离线缓存

**建议**：在 Actions 中增加 HTML/CSS/JS 压缩步骤，或引入 Service Worker 实现离线访问。

### 7.3 🟢 后台管理：每次标签切换都重新获取数据

**位置**：`Tsinhoht.html:443-446`

**问题**：切换标签页时每次都重新请求 API 数据（`loadPosts`、`loadComments`、`loadStats`），没有缓存。

**影响**：频繁切换标签页浪费带宽，API 响应慢时体验差。

**建议**：对非实时数据（帖子列表、评论列表）做简单的内存缓存，设置 30 秒过期。

---

## 8. 内容质量

### 8.1 🟠 链接有效性：部分外部链接未验证

**问题**：
- 顶部导航栏 `http://i8c.cn/45DrJ`（备用合集）使用 **HTTP** 而非 HTTPS，存在安全风险
- 各帖子中的下载链接未做定期有效性检查，用户可能遇到失效链接

**影响**：HTTP 链接在浏览器中会被标记为"不安全"，影响用户信任。

**建议**：更换为 HTTPS 链接，或增加链接有效性自动检测机制。

### 8.2 🟡 图片：无自适应分辨率

**问题**：所有封面图片固定为 `width:60px;height:60px`（首页）和 `width:54px;height:54px`（移动端），但图片源文件可能是大尺寸。没有使用 `srcset` 或 `<picture>` 元素提供不同分辨率的图片。

**影响**：移动端加载大图浪费带宽，Retina 屏上图片模糊。

**建议**：gen.js 生成不同尺寸的缩略图，使用 `srcset` 提供自适应。

### 8.3 🟢 文本错误：首页"8月11号"的日期显示为"1"月

**位置**：`index.html:118-126`，文件名 `8.1.1.html`

**问题**：`8.1.1.html` 文件名的日期解析结果：`(/(\d+)月(\d+)/)` 匹配不到 → fallback 使用文件名，显示为"1/1"。但 gen.js 处理 `dayTag` 时同样有问题（`gen.js:208-211`）：`8.1.1` 匹配不到日期格式，标题显示异常。

**影响**：首页日期显示为"1月"（本应是"8月11日"）

**建议**：修复 `dayTag` 函数，添加对 `8.X.Y` 格式的支持（`8.1.1` → `8月11日`）。

---

## 改进优先级总表

| 优先级 | 问题 | 影响 | 改动量 |
|--------|------|------|--------|
| 🔴 P0 | 评论区旧数据 pid 修复 | 核心功能缺陷 | 中（SQL + 手动确认） |
| 🔴 P0 | 统计页面 HTML 重复元素 | 界面异常 | 小（删 2 行） |
| 🔴 P0 | CSS 重复声明（index.html） | 代码质量 | 小（gen.js 修复 1 行） |
| 🟠 P1 | 公告弹窗无关闭记忆 | 体验差 | 小（加 localStorage） |
| 🟠 P1 | 8.1.1.html 日期解析错误 | 首页显示异常 | 中（改 gen.js dayTag） |
| 🟠 P1 | 表单提交防抖 | 数据安全 | 小（加锁） |
| 🟠 P1 | 颜色对比度不足 | 可访问性 | 小（改颜色值） |
| 🟠 P1 | 模版硬编码 8.10.html | 鲁棒性 | 中（加 fallback） |
| 🟡 P2 | 搜索功能增强 | 功能完善 | 中（接 fuse.js） |
| 🟡 P2 | 内联样式迁移到 CSS 文件 | 维护性 | 大（重构） |
| 🟡 P2 | 评论加载骨架屏 | 体验 | 小（加 loading） |
| 🟡 P2 | 移除幕布内嵌字体 | 性能 | 小（gen.js 过滤） |
| 🟡 P2 | 记忆搜索加载按钮位置 | 体验 | 小（改 JS） |
| 🟢 P3 | 添加 ARIA 属性 | 可访问性 | 中（逐个加） |
| 🟢 P3 | 响应式断点丰富 | 体验 | 中（加 2 断点） |
| 🟢 P3 | 焦点管理 | 可访问性 | 中（加 focus trap） |
| 🟢 P3 | Service Worker 离线缓存 | 性能 | 大（加 sw.js） |

---

## 总结

MRHYFX 是一个功能完整的静态黄油分享站，核心的发布-生成-部署流程运转良好，评论区、搜索、统计等附加功能也都实现了基本可用。最大的亮点是**纯前端管理后台 + GitHub API 原子提交 + Actions 自动生成**的闭环工作流，非常巧妙。

主要问题集中在：
1. **可维护性**：CSS/JS 分散在多处，全局变量暴露，缺乏模块化
2. **可访问性**：ARIA 属性缺失、颜色对比度不足、键盘导航不完整
3. **数据一致性**：旧评论 pid 数据污染、日期解析错误
4. **体验细节**：公告弹窗烦人、加载无反馈、搜索能力弱

建议优先修复 P0 和 P1 级别的 8 个问题，这些改动量小、影响大、收益高。