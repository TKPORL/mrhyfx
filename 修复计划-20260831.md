# 网站问题排查与修复计划

> 生成时间：2026-08-31
> 仓库：`mrhyfx`（Tsinho 黄油站）
> 状态：方案已批准，本文档落盘后按第 3 节顺序执行

---

## 0. 用户已知问题

1. 后台搜索搜不到每个帖子里面的 **游戏名称**，也搜不到帖子正文内容。
2. 后台「今日访问」一直是 0，「累计访问」不增加（用户已确认 SQL 查 `page_views` 表有数据，所以**不是写入问题，是后台读取/配置问题**）。

## 0.1 排查时发现的附加症状

- F12 Network 面板看不到 `rpc/inc_page_view` / `rpc/inc_daily_view` 请求 → **默认过滤是 "Doc" / "All"，不会显示 `fetch`**。需要切到 `Fetch/XHR` 或 `All` 才能看到。这本身不是 bug，但用户一度怀疑脚本没跑。
- `gen.js` 是覆盖式构建脚本，所有帖子 HTML 由它生成；用户**手改过**某些帖子 HTML 的，下一次构建会被覆盖回去（开工前 `git status` 已确认工作区干净）。
- `search_index.json` 已经包含每个帖子的游戏条目（`scripts/gen.js:1139`），但后台 `applySearchFilter` **不读它**，只读 `titles.json` 手维护字典。

---

## 1. 根因分析

### 1.1 后台搜索搜不到游戏名 / 帖子内容

**根因**：`Tsinhoht.html:1684-1706` `applySearchFilter()` 只在以下字段上做 `includes()` 匹配：

```js
const hay = (tag + ' ' + p.path + ' ' + displayTitle + ' ' + (titles[tag] || '')).toLowerCase();
```

字段来源：

| 字段 | 来源 | 是否含游戏名 |
|---|---|---|
| `tag` | 文件名（如 `8.10`） | ❌ |
| `p.path` | GitHub tree 路径 | ❌ |
| `displayTitle` | `titles.json`（手维护的中文标题字典） | ❌ 只含帖子标题 |
| `titles[tag]` | 同上 | ❌ |

**`titles.json` 结构**：`{ "8.10": "8月10号黄油 92款（PC+安卓）..." }`，key 是文件名去后缀，value 是中文标题字符串，**完全不含游戏名清单**。

而 `search_index.json`（`gen.js:1408` 生成，1139 行 push 每个帖子的游戏条目）里**包含**每个游戏的 title/intro/img/plat/links，**但后台没有读它**。

### 1.2 「今日访问 / 累计访问」一直是 0

用户已验证：
- `SELECT * FROM page_views LIMIT 5;` **有数据出来** → 写入端没问题。
- 后台统计页 `loadStats()` **仍然显示 0** → 读取端有问题。

后台读取走的是用户在后台「评论管理」页签填的 `sbUrl` + `sbAnon` + `sbSecret`，通过 REST 直查：

- `Tsinhoht.html:2132` `fetch(.../page_views?select=url,count&order=count.desc)` → 累加给 `#statTotalViews`
- `Tsinhoht.html:2167` `fetch(.../daily_page_views?...&day=eq.${today})` → 累加给 `#statTodayViews`

读取失败的所有路径都被 `try { ... } catch (e) {}` **静默吞掉**，用户看不到任何错误信息。

**最可能的根因**（按概率排序）：

1. **后台填的 Project URL 跟写入端内联的不是同一个 supabase 项目**。
   - 写入端（`8.10.html:1775` 等所有帖子）内联的是 `https://kydmccknlbpczeqppbtc.supabase.co` + anon key `sb_publishable_OfNqFNohTdrmzZ8dtu5bZQ_LcTk5iF9`。
   - 后台 `sbUrl` 是用户在 `Tsinhoht.html:291-301` 输入框里填的，存在 localStorage `mrhx_sb_url`。
2. **后台填的 `sbSecret`（service_role key）错了或失效**。
3. `valid` 过滤过严（已通过代码路径分析排除）。

**修复策略**：不直接猜根因，让代码自己把根因显示出来——把所有静默 `catch (e) {}` 改成 `console.error` 并显示到 box 上。这样用户刷新后台就能立刻看到具体原因。

### 1.3 其他隐患

| # | 隐患 | 严重度 |
|---|---|---|
| A | `scripts/gen.js:393` 写入端 `day` 用 UTC 切日 | 🟠 高 |
| B | `gen.js:399` `inc_daily_view` 静默 catch | 🟠 高 |
| C | `inc_page_view` / `inc_daily_view` RPC 在 `supabase/` 下**没有对应 SQL 文件** | 🟠 高 |
| D | `page_views` / `daily_page_views` / `download_clicks` 没有索引 | 🟡 中 |

---

## 2. 修复方案

### 修复 1：后台搜索支持游戏名 / 帖子内容

- `scripts/gen.js` 新增输出 `game_index.json`，结构：`{ "8.10": { title, games:["露露的城市建设", ...], intro }, ... }`
- `Tsinhoht.html:applySearchFilter()` 改用 `game_index`，匹配字段升级为 `tag + path + title + games.join(' ') + intro`
- `loadPosts()` 增加 `game_index.json` 拉取

### 修复 2：访问量为 0

**Step A**：改 `loadStats()` / `loadDaily()` / `loadSnapshots()`，暴露 fetch 错误。

**Step B**：新增 `supabase/upgrade_page_views.sql`，包含 `page_views` / `daily_page_views` 建表 + 索引 + `inc_page_view` / `inc_daily_view` RPC + `GRANT EXECUTE TO anon`。

### 修复 3：写入端切日改北京时间

`gen.js:393`：

```js
var day = new Date(Date.now() + 8 * 3600000).toISOString().slice(0, 10);
```

去掉 `gen.js:399` 的静默 `.catch`。

### 修复 4：文档

新增 `scripts/README.md`：说明 `node scripts/gen.js` 会覆盖所有帖子 HTML，手改前要 commit。

---

## 3. 实施步骤

1. 改 `Tsinhoht.html` 三处错误处理（暴露 fetch 失败）
2. 改 `scripts/gen.js`（输出 `game_index.json` + 时区 + 去掉静默 catch）
3. 改 `Tsinhoht.html:applySearchFilter()` / `loadPosts()` 读 `game_index.json`
4. 新增 `supabase/upgrade_page_views.sql`
5. 新增 `scripts/README.md`
6. 执行 `node scripts/gen.js`
7. **用户**在 Supabase Dashboard SQL Editor 跑 `supabase/upgrade_page_views.sql`
8. 浏览器验证（F12 Network / 后台统计 / 后台搜索）
9. 分 2 个 commit：
   - `fix: 后台搜索支持游戏名/帖子内容搜索`
   - `fix: 访问量为 0 问题排查（暴露错误 + 补齐 supabase RPC SQL + 写入端改北京时间切日）`

---

## 4. 风险与回滚

| 风险 | 回滚方式 |
|---|---|
| `gen.js` 重跑覆盖手改内容 | `git checkout HEAD -- <file>` |
| 新 `game_index.json` 字段缺失 | 看 `gen.js` 输出日志，补提取规则 |
| `loadStats` 暴露错误后控制台噪音大 | 仅 console.error，不弹窗 |
| 用户没去 Supabase 跑 SQL | 仅控制台报错，不阻塞 commit |

---

## 5. 不在本次范围

- 前台 `search.html` 搜索体验升级（拼音/同义词/双关键词）
- `Tsinhoht.html` 后台拆分重构
- 把搜索迁到 Supabase `tsvector`
- `search.html` / `jinri.html` 是否注入访问追踪
